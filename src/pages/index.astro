---
import MainLayout from "../layouts/MainLayout.astro";
import ProjectShowcase from "../components/ProjectShowcase.tsx";
import SkillsList from "../components/SkillsList.tsx";
import TerminalHero from "../components/TerminalHero.tsx";
import Starfield from "../components/Starfield.tsx";
import ExperienceTimeline from "../components/ExperienceTimeline.tsx";
import { FaHandSparkles, FaArrowRight, FaEnvelope, FaWhatsapp, FaPhone } from "react-icons/fa";
---

<MainLayout>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Hero Section -->
        <section
            id="hero"
            class="min-h-[calc(100vh-8rem)] md:min-h-[calc(100vh-10rem)] flex flex-col justify-center items-center text-center relative"
        >
            <Starfield client:load />
            <TerminalHero client:load />
            <div class="mt-10 flex flex-col sm:flex-row items-center gap-4 sm:gap-x-6">
                <a
                    href="#contact"
                    class="rounded-full bg-primary/10 px-6 py-3 text-base font-bold text-primary border border-primary/50 shadow-[0_0_20px_rgba(var(--color-primary),0.3)] hover:bg-primary hover:text-inverted hover:shadow-[0_0_40px_rgba(var(--color-primary),0.6)] hover:scale-105 transition-all duration-300"
                    >Contáctame</a
                >
                <a
                    href="#projects"
                    class="rounded-full bg-transparent px-6 py-3 text-base font-bold text-primary border border-primary/30 shadow-[0_0_10px_rgba(var(--color-primary),0.1)] hover:bg-primary/10 hover:border-primary/80 hover:shadow-[0_0_20px_rgba(var(--color-primary),0.4)] hover:scale-105 transition-all duration-300 flex items-center gap-2"
                    >Ver mis proyectos <FaArrowRight
                        client:load
                        className="group-hover:translate-x-1 transition-transform"
                    /></a
                >
            </div>
        </section>

        <!-- About Section -->
        <section id="about" class="py-16 md:py-24 relative">
            <!-- Decorative background blur -->
            <div
                class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[120%] bg-primary/5 blur-[100px] -z-10 rounded-full pointer-events-none"
            >
            </div>

            <div
                class="neon-box p-8 md:p-12 rounded-3xl max-w-4xl mx-auto text-center transform hover:scale-[1.01] transition-transform duration-500"
            >
                <h2 class="section-title">Sobre Mí</h2>
                <p
                    class="text-lg md:text-xl leading-relaxed text-muted dark:text-secondary/90 max-w-3xl mx-auto"
                >
                    Soy un apasionado por la tecnología y el desarrollo de
                    software, con un fuerte enfoque en la arquitectura frontend,
                    la automatización de procesos DevOps y la seguridad web.
                    Disfruto liderando equipos técnicos, mentorizando a
                    desarrolladores y participando activamente en revisiones de
                    código para mantener altos estándares de calidad. <br /><br
                    />
                    <span class="text-primary font-medium"
                        >Mi motivación es identificar trabajo de alto impacto</span
                    >, priorizando siempre el valor para el cliente, y me
                    mantengo en continuo aprendizaje, explorando nuevas
                    tecnologías como <span
                        class="text-accent underline decoration-accent/30 underline-offset-4"
                        >microfrontends</span
                    > y el ecosistema de
                    <span
                        class="text-accent underline decoration-accent/30 underline-offset-4"
                        >IA Generativa</span
                    >.
                </p>
            </div>
        </section>

        <!-- Experience Section -->
        <section id="experience" class="py-12 md:py-16">
            <h2 class="section-title">Experiencia Profesional</h2>
            <ExperienceTimeline client:only="react" />
        </section>

        <!-- Projects Section -->
        <section id="projects" class="py-12 md:py-16">
            <h2 class="section-title">Proyectos</h2>

            <div class="mt-12">
                <ProjectShowcase client:visible />
            </div>
        </section>

        <!-- Skills Section -->
        <section id="skills" class="py-12 md:py-16">
            <h2 class="section-title">Habilidades Técnicas</h2>
            <div class="mt-8">
                <SkillsList client:visible />
            </div>
        </section>

        <!-- Contact Section -->
        <section id="contact" class="py-16 sm:py-24">
            <div
                class="neon-box p-8 md:p-16 rounded-3xl max-w-4xl mx-auto text-center"
            >
                <h2 class="section-title mb-6">Ponte en Contacto</h2>
                <p
                    class="text-lg md:text-xl leading-relaxed text-muted dark:text-secondary/90 max-w-2xl mx-auto mb-10"
                >
                    Estoy abierto a nuevas oportunidades y colaboraciones. Si
                    tienes un proyecto en mente o simplemente quieres saludar,
                    no dudes en escribirme.
                </p>

                <div class="flex flex-wrap justify-center gap-4">
                    <a
                        href="mailto:oscarsinuco@gmail.com"
                        class="inline-flex items-center justify-center gap-2 rounded-full bg-primary/10 px-6 py-3 text-base font-bold text-primary border border-primary/50 shadow-[0_0_20px_rgba(var(--color-primary),0.3)] hover:bg-primary hover:text-inverted hover:shadow-[0_0_40px_rgba(var(--color-primary),0.6)] hover:scale-105 transition-all duration-300"
                    >
                        <FaEnvelope client:load /> Gmail
                    </a>
                    <a
                        href="https://wa.me/573154573647"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="inline-flex items-center justify-center gap-2 rounded-full bg-primary/10 px-6 py-3 text-base font-bold text-primary border border-primary/50 shadow-[0_0_20px_rgba(var(--color-primary),0.3)] hover:bg-primary hover:text-inverted hover:shadow-[0_0_40px_rgba(var(--color-primary),0.6)] hover:scale-105 transition-all duration-300"
                    >
                        <FaWhatsapp client:load /> WhatsApp
                    </a>
                    <a
                        href="tel:+573154573647"
                        class="inline-flex items-center justify-center gap-2 rounded-full bg-primary/10 px-6 py-3 text-base font-bold text-primary border border-primary/50 shadow-[0_0_20px_rgba(var(--color-primary),0.3)] hover:bg-primary hover:text-inverted hover:shadow-[0_0_40px_rgba(var(--color-primary),0.6)] hover:scale-105 transition-all duration-300"
                    >
                        <FaPhone client:load /> Llamar
                    </a>
                </div>
            </div>
        </section>
    </div>
</MainLayout>
